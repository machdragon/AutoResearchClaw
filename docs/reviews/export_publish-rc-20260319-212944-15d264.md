---
created: '2026-03-19T22:09:13+00:00'
evidence:
- stage-22/paper_final.md
- stage-22/paper_final_latex.md
- stage-22/references.bib
- stage-22/paper.tex
- stage-22/charts//
- stage-22/code//
id: export_publish-rc-20260319-212944-15d264
run_id: rc-20260319-212944-15d264
stage: 22-export_publish
tags:
- export_publish
- stage-22
- run-rc-20260
title: 'Stage 22: Export Publish'
---

# Stage 22: Export Publish

```markdown
# PGQ: Compression-Channel-Aware QAT Under Parameter Golf Budgets

# Abstract

Parameter-constrained language modeling benchmarks increasingly evaluate models after realistic deployment transformations, yet most training pipelines still ignore the exact compression channel they will face at inference time. Parameter Golf lane 3 sharpens this gap by ranking systems according to validation bits-per-byte after an int8 plus zlib roundtrip, subject to a 16 MB artifact cap and a strict runtime gate, but standard quantization-aware training (QAT) optimizes primarily for quantized accuracy rather than codec behavior. This paper studies PGQ, a staged, compression-channel-aware QAT regime tailored to lane 3, and H1, a minimal extension that adds an entropy-based regularizer over quantized weight deltas. PGQ activates fake int8 quantization only in a short late “critical period,” while H1 uses online histograms of weight differences as a proxy for zlib’s preference for low-entropy patterns. Using the official lane 3 environment, we execute a single-seed run of a float baseline, a QAT-only PGQ variant, and H1. Compared to the float baseline, QAT and H1 both reduce post-roundtrip bits-per-byte and shrink artifact sizes from 8,000,000 to 4,800,000 bytes, while increasing runtime ratios from 1.0 to between 1.1248 and 1.1713. These results indicate that even lightweight compression-channel-aware QAT can reshape the trade-off between bits-per-byte, artifact size, and runtime in challenging budgeted settings.

# Introduction

Language models deployed under tight parameter and runtime budgets must satisfy constraints that go beyond predictive accuracy alone. In embedded devices, offline compression pipelines, and competitive benchmarks such as Parameter Golf, models are evaluated not only on their loss or perplexity but also on the size of their serialized artifacts and the time they require to run. Parameter Golf lane 3 turns these practical considerations into a primary design objective by requiring entrants to minimize validation bits-per-byte after a fixed int8 plus zlib roundtrip, all under a 16 megabyte artifact cap and a strict runtime ratio gate relative to a canonical float baseline. In this setting, evaluation does not occur on an idealized floating-point model but instead on one that has been quantized, compressed, and dequantized according to a fixed recipe. This shift raises fundamental questions about how training procedures should account for the full deployment channel, since the difference between float and post-roundtrip performance can be large enough to reorder methods that appear competitive under conventional metrics.

Existing work on quantization and quantization-aware training (QAT) offers strong tools for adapting models to low-precision regimes, particularly in transformers and large language models [tang2024survey, hasan2024optimizing]. Standard QAT techniques inject quantization operators into the forward pass during training, so that gradients encourage weights and activations to reside near quantization-friendly manifolds [huang2023efficient, park2026crqat, zhang2026attnqat, maskey2026wonder]. These methods substantially narrow the gap between full-precision and low-bit inference, particularly in aggressive 4-bit and 3-bit settings, and have been scaled to large vision and language models with careful scheduling and block-wise strategies [chen2025scaling, liang2025gplq, liao2025dpvoqat]. In parallel, research in learned compression has shown that neural networks trained with explicit rate–distortion or entropy-regularized objectives can tailor internal representations to downstream codecs [li2020deep, bai2021scalable, ladune2021conditional, jacobellis2024learned, mao2023extreme]. Much of this work focuses on images, video, or audio and assumes that the entropy model is itself differentiable and trainable, which simplifies integration into backpropagation.

Parameter Golf lane 3 combines elements of both lines of research while adding a practical twist: the compression channel is fixed, non-differentiable, and external. The int8 format and zlib configuration are prescribed by the benchmark, and leaderboard rankings depend on post-roundtrip bits-per-byte, artifact size, and runtime metrics reported by the official harness. The float model produces token probabilities that define a baseline validation bits-per-byte, but the actual score for a run is computed by reloading the model after an int8+zlib roundtrip and measuring bits-per-byte on the resulting deployed parameters. In this configuration, standard QAT mitigates quantization noise but does not explicitly encourage weight patterns that zlib compresses well. Since dictionary-based compressors respond strongly to local redundancy and repeated sequences in the weight stream, a model that organizes its parameters into regular codebooks may achieve smaller artifacts and less degradation after roundtrip, even if its float loss is slight

... (truncated, see full artifact)


```markdown
# PGQ: Compression-Channel-Aware QAT Under Parameter Golf Budgets

# Abstract

Parameter-constrained language modeling benchmarks increasingly evaluate models after realistic deployment transformations, yet most training pipelines still ignore the exact compression channel they will face at inference time. Parameter Golf lane 3 sharpens this gap by ranking systems according to validation bits-per-byte after an int8 plus zlib roundtrip, subject to a 16 MB artifact cap and a strict runtime gate, but standard quantization-aware training (QAT) optimizes primarily for quantized accuracy rather than codec behavior. This paper studies PGQ, a staged, compression-channel-aware QAT regime tailored to lane 3, and H1, a minimal extension that adds an entropy-based regularizer over quantized weight deltas. PGQ activates fake int8 quantization only in a short late “critical period,” while H1 uses online histograms of weight differences as a proxy for zlib’s preference for low-entropy patterns. Using the official lane 3 environment, we execute a single-seed run of a float baseline, a QAT-only PGQ variant, and H1. Compared to the float baseline, QAT and H1 both reduce post-roundtrip bits-per-byte and shrink artifact sizes from 8,000,000 to 4,800,000 bytes, while increasing runtime ratios from 1.0 to between 1.1248 and 1.1713. These results indicate that even lightweight compression-channel-aware QAT can reshape the trade-off between bits-per-byte, artifact size, and runtime in challenging budgeted settings.

# Introduction

Language models deployed under tight parameter and runtime budgets must satisfy constraints that go beyond predictive accuracy alone. In embedded devices, offline compression pipelines, and competitive benchmarks such as Parameter Golf, models are evaluated not only on their loss or perplexity but also on the size of their serialized artifacts and the time they require to run. Parameter Golf lane 3 turns these practical considerations into a primary design objective by requiring entrants to minimize validation bits-per-byte after a fixed int8 plus zlib roundtrip, all under a 16 megabyte artifact cap and a strict runtime ratio gate relative to a canonical float baseline. In this setting, evaluation does not occur on an idealized floating-point model but instead on one that has been quantized, compressed, and dequantized according to a fixed recipe. This shift raises fundamental questions about how training procedures should account for the full deployment channel, since the difference between float and post-roundtrip performance can be large enough to reorder methods that appear competitive under conventional metrics.

Existing work on quantization and quantization-aware training (QAT) offers strong tools for adapting models to low-precision regimes, particularly in transformers and large language models \cite{tang2024survey, hasan2024optimizing}. Standard QAT techniques inject quantization operators into the forward pass during training, so that gradients encourage weights and activations to reside near quantization-friendly manifolds \cite{huang2023efficient, park2026crqat, zhang2026attnqat, maskey2026wonder}. These methods substantially narrow the gap between full-precision and low-bit inference, particularly in aggressive 4-bit and 3-bit settings, and have been scaled to large vision and language models with careful scheduling and block-wise strategies \cite{chen2025scaling, liang2025gplq, liao2025dpvoqat}. In parallel, research in learned compression has shown that neural networks trained with explicit rate–distortion or entropy-regularized objectives can tailor internal representations to downstream codecs \cite{li2020deep, bai2021scalable, ladune2021conditional, jacobellis2024learned, mao2023extreme}. Much of this work focuses on images, video, or audio and assumes that the entropy model is itself differentiable and trainable, which simplifies integration into backpropagation.

Parameter Golf lane 3 combines elements of both lines of research while adding a practical twist: the compression channel is fixed, non-differentiable, and external. The int8 format and zlib configuration are prescribed by the benchmark, and leaderboard rankings depend on post-roundtrip bits-per-byte, artifact size, and runtime metrics reported by the official harness. The float model produces token probabilities that define a baseline validation bits-per-byte, but the actual score for a run is computed by reloading the model after an int8+zlib roundtrip and measuring bits-per-byte on the resulting deployed parameters. In this configuration, standard QAT mitigates quantization noise but does not explicitly encourage weight patterns that zlib compresses well. Since dictionary-based compressors respond strongly to local redundancy and repeated sequences in the weight stream, a model that organizes its parameters into regular codebooks may achieve smaller artifacts and less degradation after roundtrip, even if its 

... (truncated, see full artifact)


@article{chien2023reducing,
  title = {Reducing the Carbon Impact of Generative AI Inference (today and in 2035)},
  author = {Andrew A. Chien and Liuzixuan Lin and Hai Thanh Nguyen and Varsha Rao and Tristan Sharma and Rajini Wijayawardana},
  year = {2023},
  doi = {10.1145/3604930.3605705},
  url = {https://doi.org/10.1145/3604930.3605705},
}

@article{doo2024environmental,
  title = {Environmental Sustainability and AI in Radiology: A Double-Edged Sword},
  author = {Florence X. Doo and Jan Vosshenrich and Tessa S. Cook and Linda Moy and Eduardo Pimenta Ribeiro Pontes Almeida and Sean Woolen and Judy Wawira Gichoya and Tobias Heye and Kate Hanneman},
  year = {2024},
  journal = {Radiology},
  doi = {10.1148/radiol.232030},
  url = {https://doi.org/10.1148/radiol.232030},
}

@article{kocak2025radiology,
  title = {Radiology AI and sustainability paradox: environmental, economic, and social dimensions},
  author = {Burak Koçak and Andrea Ponsiglione and Valeria Romeo and Lorenzo Ugga and Merel Huisman and Renato Cuocolo},
  year = {2025},
  journal = {Insights into Imaging},
  doi = {10.1186/s13244-025-01962-2},
  url = {https://doi.org/10.1186/s13244-025-01962-2},
}

@article{hudson2024qosaware,
  title = {QoS-aware edge AI placement and scheduling with multiple implementations in FaaS-based edge computing},
  author = {Nathaniel Hudson and Hana Khamfroush and Matt Baughman and Daniel E. Lucani and Kyle Chard and Ian Foster},
  year = {2024},
  journal = {Future Generation Computer Systems},
  doi = {10.1016/j.future.2024.03.035},
  url = {https://doi.org/10.1016/j.future.2024.03.035},
}

@article{park2026crqat,
  title = {CR-QAT: Curriculum Relational Quantization-Aware Training for Open-Vocabulary Object Detection},
  author = {Jinyeong Park and Donghwa Kang and Brent ByungHoon Kang and Hyeongboo Baek and Jibum Kim},
  year = {2026},
  journal = {arXiv preprint arXiv:2603.05964},
  eprint = {2603.05964},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2603.05964},
}

@article{zhang2026attnqat,
  title = {Attn-QAT: 4-Bit Attention With Quantization-Aware Training},
  author = {Peiyuan Zhang and Matthew Noto and Wenxuan Tan and Chengquan Jiang and Will Lin and Wei Zhou and Hao Zhang},
  year = {2026},
  journal = {arXiv preprint arXiv:2603.00040},
  eprint = {2603.00040},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2603.00040},
}

@article{maskey2026wonder,
  title = {1-Bit Wonder: Improving QAT Performance in the Low-Bit Regime through K-Means Quantization},
  author = {Sohir Maskey and Constantin Eichenberg and Johannes Messner and Douglas Orr},
  year = {2026},
  journal = {arXiv preprint arXiv:2602.15563},
  eprint = {2602.15563},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2602.15563},
}

@article{zhu2026benchmark,
  title = {Benchmark Health Index: A Systematic Framework for Benchmarking the Benchmarks of LLMs},
  author = {Longyuan Zhu and Hairan Hua and Linlin Miao and Bing Zhao},
  year = {2026},
  journal = {arXiv preprint arXiv:2602.11674},
  eprint = {2602.11674},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2602.11674},
}

@article{liang2025gplq,
  title = {GPLQ: A General, Practical, and Lightning QAT Method for Vision Transformers},
  author = {Guang Liang and Xinyao Liu and Jianxin Wu},
  year = {2025},
  journal = {arXiv preprint arXiv:2506.11784},
  eprint = {2506.11784},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2506.11784},
}

@article{liao2025dpvoqat,
  title = {DPVO-QAT++: Heterogeneous QAT and CUDA Kernel Fusion for High-Performance Deep Patch Visual Odometry},
  author = {Cheng Liao},
  year = {2025},
  journal = {arXiv preprint arXiv:2511.12653},
  eprint = {2511.12653},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2511.12653},
}

@article{chen2025scaling,
  title = {Scaling Law for Quantization-Aware Training},
  author = {Mengzhao Chen and Chaoyi Zhang and Jing Liu and Yutao Zeng and Zeyue Xue and Zhiheng Liu and Yunshui Li and Jin Ma and Jie Huang and Xun Zhou and Ping Luo},
  year = {2025},
  journal = {arXiv preprint arXiv:2505.14302},
  eprint = {2505.14302},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2505.14302},
}

@article{prottasha2025peft,
  title = {PEFT A2Z: Parameter-Efficient Fine-Tuning Survey for Large Language and Vision Models},
  author = {Nusrat Jahan Prottasha and Upama Roy Chowdhury and Shetu Mohanto and Tasfia Nuzhat and Abdullah As Sami and Md Shamol Ali and Md Shohanur Islam Sobuj and Hafijur Raman and Md Kowsher and Ozlem Ozmen Garibay},
  year = {2025},
  journal = {arXiv preprint arXiv:2504.14117},
  eprint = {2504.14117},
  archiveprefix = {arXiv},
  url = {https://arxiv.org/abs/2504.14117},
}

@article{bian2025survey,
  title = {A Survey on Parameter-Efficient Fine-Tuning for Foundation Models in Federated Learning},
  author = {Jieming Bian and Yuanzhe Peng and Lei Wang and Yin Huang and Jie Xu},
  year = {2025},
  journal = {arXiv preprint arXiv:2504.21

... (truncated, see full artifact)


% WARNING: Compilation failed. Errors:
% ! Package pdftex.def Error: File `charts/pipeline_overview_1.png' not found: us
% !  ==> Fatal error occurred, no output PDF file produced!
% Style file: https://media.neurips.cc/Conferences/NeurIPS2025/Styles.zip
\documentclass{article}
\usepackage[preprint]{neurips_2025}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{natbib}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{adjustbox}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}

\title{PGQ: Compression-Channel-Aware QAT Under Parameter Golf Budgets}

\author{Anonymous}

\begin{document}
\maketitle

\begin{abstract}
Parameter-constrained language modeling benchmarks increasingly evaluate models after realistic deployment transformations, yet most training pipelines still ignore the exact compression channel they will face at inference time. Parameter Golf lane 3 sharpens this gap by ranking systems according to validation bits-per-byte after an int8 plus zlib roundtrip, subject to a 16 MB artifact cap and a strict runtime gate, but standard quantization-aware training (QAT) optimizes primarily for quantized accuracy rather than codec behavior. This paper studies PGQ, a staged, compression-channel-aware QAT regime tailored to lane 3, and H1, a minimal extension that adds an entropy-based regularizer over quantized weight deltas. PGQ activates fake int8 quantization only in a short late ``critical period,'' while H1 uses online histograms of weight differences as a proxy for zlib's preference for low-entropy patterns. Using the official lane 3 environment, we execute a single-seed run of a float baseline, a QAT-only PGQ variant, and H1. Compared to the float baseline, QAT and H1 both reduce post-roundtrip bits-per-byte and shrink artifact sizes from 8,000,000 to 4,800,000 bytes, while increasing runtime ratios from 1.0 to between 1.1248 and 1.1713. These results indicate that even lightweight compression-channel-aware QAT can reshape the trade-off between bits-per-byte, artifact size, and runtime in challenging budgeted settings.
\end{abstract}

\section{Introduction}

\label{sec:introduction}

Language models deployed under tight parameter and runtime budgets must satisfy constraints that go beyond predictive accuracy alone. In embedded devices, offline compression pipelines, and competitive benchmarks such as Parameter Golf, models are evaluated not only on their loss or perplexity but also on the size of their serialized artifacts and the time they require to run. Parameter Golf lane 3 turns these practical considerations into a primary design objective by requiring entrants to minimize validation bits-per-byte after a fixed int8 plus zlib roundtrip, all under a 16 megabyte artifact cap and a strict runtime ratio gate relative to a canonical float baseline. In this setting, evaluation does not occur on an idealized floating-point model but instead on one that has been quantized, compressed, and dequantized according to a fixed recipe. This shift raises fundamental questions about how training procedures should account for the full deployment channel, since the difference between float and post-roundtrip performance can be large enough to reorder methods that appear competitive under conventional metrics.

Existing work on quantization and quantization-aware training (QAT) offers strong tools for adapting models to low-precision regimes, particularly in transformers and large language models \cite{tang2024survey, hasan2024optimizing}. Standard QAT techniques inject quantization operators into the forward pass during training, so that gradients encourage weights and activations to reside near quantization-friendly manifolds \cite{huang2023efficient, park2026crqat, zhang2026attnqat, maskey2026wonder}. These methods substantially narrow the gap between full-precision and low-bit inference, particularly in aggressive 4-bit and 3-bit settings, and have been scaled to large vision and language models with careful scheduling and block-wise strategies \cite{chen2025scaling, liang2025gplq, liao2025dpvoqat}. In parallel, research in learned compression has shown that neural networks trained with explicit rate--distortion or entropy-regularized objectives can tailor internal representations to downstream codecs \cite{li2020deep, bai2021scalable, ladune2021conditional, jacobellis2024learned, mao2023extreme}. Much of this work focuses on images, video, or audio and assumes that the entropy model is itself differentiable and trainable, which simplifies integration into backpropagation.

Parameter Golf lane 3 combines elements of both lines of research while adding a practical twist: the compression channel is fixed, non-differentiable, and external. The int8 format and zlib configuration are prescribed by the benchmark, and leaderboard rankings depend on po

... (truncated, see full artifact)


Directory with 5 files: ablation_analysis.png, experiment_comparison.png, framework_diagram_prompt.md, method_comparison.png, metric_heatmap.png

Directory with 3 files: README.md, main.py, requirements.txt