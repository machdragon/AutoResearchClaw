---
created: '2026-03-19T20:18:27+00:00'
evidence:
- stage-16/outline.md
id: paper_outline-rc-20260319-193805-15d264
run_id: rc-20260319-193805-15d264
stage: 16-paper_outline
tags:
- paper_outline
- stage-16
- run-rc-20260
title: 'Stage 16: Paper Outline'
---

# Stage 16: Paper Outline

```markdown
# Candidate Titles

1. **QZIP: Codec-Aligned Quantization-Aware Training for Parameter Golf**
   - Memorability: 5/5
   - Specificity: 3/5
   - Novelty signal: 4/5

2. **CRQ: Codec-Roundtrip Quantization for Artifact-Limited Parameter Golf Models**
   - Memorability: 4/5
   - Specificity: 4/5
   - Novelty signal: 4/5

3. **ZQAT: Zlib-Roundtrip Quantization-Aware Training under Strict Model Budgets**
   - Memorability: 4/5
   - Specificity: 5/5
   - Novelty signal: 4/5

In the outline below I will use **ZQAT** as the working method name; swapping to QZIP or CRQ is trivial later.

---

# Paper Outline

## Abstract (≈200 words)

**Goals:**  
Summarize the Parameter Golf setting, the deployment-aligned quantization problem, the ZQAT idea (staged QAT with explicit int8+zlib roundtrip in the loop), and the key *planned* empirical claims. Since no results exist yet, the abstract will be written in a way that can later be populated with real numbers.

**Key elements to include:**
- Problem: compressing models to ≤16MB with ≤1.10× runtime while preserving post-roundtrip `val_bpb`.
- Gap: standard QAT/PTQ ignore codec specifics; Parameter Golf lane_3 calls for codec-aware training.
- Method: ZQAT, which embeds the int8+zlib+de-zlib roundtrip into a staged QAT regime under a fixed gate budget.
- Results (future insertion): relative and absolute improvements vs float+PTQ+zlib and staged-QAT-without-codec baselines, all respecting Parameter Golf constraints.
- Takeaway: codec-aligned QAT can improve deployment-metric `val_bpb` without violating tight artifact/runtime gates.

**Evidence links (for later):**
- Pointer to Parameter Golf benchmark documentation and leaderboard.
- Pointer to records-folder submission IDs used in the paper.

---

## 1 Introduction (800–1000 words)

### 1.1 Motivation: Compression under Hard Deployment Gates

**Goals (≈200–250 words):**
- Introduce the general problem of deploying neural models under strict memory and latency constraints.
- Emphasize the Parameter Golf challenge: ≤16,000,000 bytes artifact, ≤1.10× baseline runtime, 10-minute train/eval budgets.
- Explain why `val_bpb` is the relevant metric in this benchmark (compression-aligned predictive performance).

**Evidence / references:**
- Parameter Golf challenge description and rules.
- Prior work on model compression with strict memory/latency constraints.

### 1.2 Gap: QAT and PTQ Ignore the Full Codec Roundtrip

**Goals (≈200–250 words):**
- Argue that most quantization work optimizes float-to-int mapping in isolation, assuming deployment codec is a secondary concern.
- Highlight that in Parameter Golf lane_3, deployment *is* int8 weights serialized and compressed with zlib.
- Explain conceptually why codec omission can cause misalignment: a layout that looks good in quantization error may be suboptimal for entropy coding and thus `val_bpb`.

**Evidence / references:**
- Foundations of QAT and PTQ for neural networks.
- Works on entropy-constrained quantization / model compression that nevertheless abstract away concrete codecs.
- Discussions of deployment mismatch in quantization (e.g., different kernels/codecs in production vs training).

### 1.3 Our Approach: ZQAT for Codec-Roundtrip-Aware Training

**Goals (≈200–250 words):**
- Introduce **ZQAT** by name and describe at a high level:
  - Staged QAT schedule under a fixed “gate budget” (limited number/length of quantized/codec passes).
  - Explicit integration of the roundtrip: float → int8 layout → zlib compress → zlib decompress → int8 weights used for prediction.
- Emphasize that the Parameter Golf constraints shape the design:
  - Codec calls are expensive and must be budgeted.
  - Artifact size must be measured post-zlib.
  - Evaluation uses post-roundtrip `val_bpb`.
- Clarify what lane_3 focuses on: QAT + roundtrip-aware compression alignment, not just better codecs.

**Evidence / references:**
- Prior work on deployment-aware training (e.g., training with specific hardware kernels, mixed-precision training).
- Possibly cite works on learned compression that include codec layers.

### 1.4 Contributions

**Goals (≈200–250 words):**
- Present 3–4 concrete contributions, clearly separated from future work.
- Explicitly mention alignment with Parameter Golf lane_3 and constraints.

**Planned contribution bullets:**
1. **Codec-aligned training formulation:** A formalization of QAT where the optimization objective is evaluated after an int8+zlib roundtrip, directly mirroring deployment in Parameter Golf lane_3.
2. **ZQAT algorithm:** A staged QAT procedure that schedules codec-aligned updates under a strict gate budget, balancing accuracy and runtime.
3. **Baseline and ablation suite for Parameter Golf lane_3:** A set of controlled baselines (float+PTQ+zlib, entropy-shaped PTQ, staged QAT without codec-in-loop) and ablations that isolate format, QAT, and codec-in-loop effects.
4. **Empirical evaluation under official constraints:** Demonstration that codec-roundtr

... (truncated, see full artifact)
