---
created: '2026-03-19T14:17:25+00:00'
evidence:
- stage-16/outline.md
id: paper_outline-rc-20260319-141611-2fdb44
run_id: rc-20260319-141611-2fdb44
stage: 16-paper_outline
tags:
- paper_outline
- stage-16
- run-rc-20260
title: 'Stage 16: Paper Outline'
---

# Stage 16: Paper Outline

Below is a conference-ready paper outline (Markdown) tuned to NeurIPS/ICLR/ICML standards and constrained to the Parameter Golf / 16MB artifact rules. It is explicitly designed to convert the current REFINE verdict into a clean, record‑eligible submission by (A) fixing infra/ablation bugs, (B) adding minimal statistical rigor, and (C) keeping all changes local (new experiment files, configs, records-folder outputs), per the HARD PARAMETER GOLF CONSTRAINTS.

Method name (2–5 chars): GOLF

Quick note on evidence links: where I reference run artifacts, I point to the records-folder pattern you must use for submission candidates (records/<run-id>/...). Replace <run-id> with your actual run id/timestamp. These links intentionally use the records-folder submission path required by the Parameter Golf rules.

1) Three candidate paper titles (MethodName: Subtitle) — each <= 14 words
- GOLF: Boundary‑Aware Compression for 16MB Parameter Golf
  - Memorability: 5/5
  - Specificity: 4/5
  - Novelty signal: 4/5
- GOLF: Quick‑Harness Parameter Golf with Artifact‑Size and Boundary Robustness
  - Memorability: 4/5
  - Specificity: 5/5
  - Novelty signal: 3/5
- GOLF: Joint Val‑BPB Minimization and Safety under a 16MB Artifact Cap
  - Memorability: 3/5
  - Specificity: 5/5
  - Novelty signal: 3/5

Choose the title that best matches the final emphasis (method vs. evaluation vs. safety). I recommend the first for clarity + brandability.

2) Top-level paper outline (with per-section goals, word targets, and evidence links)

- Title, Authors, Abstract (180–220 words)
  - Goal: 1–2 sentences of problem/gap; name GOLF by sentence 3; describe core idea (boundary‑aware compression controller + artifact‑aware objective); 2–3 concrete quantitative statements *sourced from instrumented runs* (once fixed).
  - Word target: 180–220
  - Evidence links:
    - Current debugging snapshot: records/<run-id>/run_summary.json (contains status: failed + error trace)
    - Current val_bpb band: records/<run-id>/metrics.csv (val_bpb ≈ 0.1239–0.1266) — use only as "sanity check" numbers until re-run with fixes.

- 1. Introduction (800–1000 words)
  - Goals:
    - Motivate Parameter Golf: strict artifact-size budgets (16MB) + fast leaderboard‑style evaluation (10 minute budgets).
    - Identify gap: existing compression work often ignores boundary‑localized safety effects and quick‑harness reproducibility under strict artifact caps.
    - Introduce GOLF: short phrase name + one-sentence contribution list.
    - State three/four contributions (design + infra + empirical: multi-seed, artifact-size vs val_bpb curves, boundary-local safety stratification).
  - Word target: 800–1,000
  - Evidence links to justify motivational claims:
    - Records showing infrastructure partly works but failed secondary metrics: records/<run-id>/run_summary.json
    - Observed val_bpb spread and broken ablations (sanity): records/<run-id>/metrics.csv
  - Citations: minimum 8–12 prior works (compression, model pruning, robustness, ML-systems, dataset compression)

- 2. Related Work (600–800 words)
  - Organization: three subsections
    - (a) model compression & artifact-limited deployment
    - (b) safety/robustness under compression / boundary-local evaluations
    - (c) fast-harness and reproducible leaderboard-style runs
  - Goal: explain differences; end each subsection with how GOLF differs
  - Word target: 600–800
  - Evidence links (for positioning):
    - Emphasize that current run lacks safety metrics (records/<run-id>/run_summary.json) and motivates adding them.

- 3. Problem Formulation & Objectives (250–350 words)
  - Goals:
    - Formal notation: dataset D, model M(θ), artifact A(M; compress_params) with size constraint |A| ≤ 16,000,000 bytes.
    - Primary objective: minimize val_bpb subject to |A| ≤ 16MB and quick-harness budget T ≤ 10 minutes per run.
    - Secondary objective: boundary-local robustness metric R_bd (define mathematically — e.g., violation rate conditional on distance to promotion boundary).
  - Word target: 250–350
  - Evidence links:
    - Cite the harness constraints and failed logs that show quick-harness runs exist (records/<run-id>/run_summary.json).

- 4. Method (GOLF) (1,000–1,500 words)
  - Structure:
    - 4.1 High-level architecture (controller, compressor, artifact synthesizer)
      - Goals: present the pipeline and the 3 levers (global vs boundary-weighted compression, hash-chaining, summary vs core artifact).
    - 4.2 Objective functions and training loop (notation + algorithm pseudocode)
      - Goals: show joint optimization of val_bpb + penalty(|A| − 16MB)+quick-harness proxy.
      - Provide algorithm environment pseudocode for the quick-harness run (init, steps, checkpointing, secondary metric evaluation).
    - 4.3 Boundary‑aware compressor design (how boundary weighting is implemented, how boundary distance is computed, and how it alters compression allocation)
    - 4.4 Practical constraints and implementation choices

... (truncated, see full artifact)
