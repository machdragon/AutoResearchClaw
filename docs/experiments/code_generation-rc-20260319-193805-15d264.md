---
created: '2026-03-19T19:57:05+00:00'
evidence:
- stage-10/experiment//
- stage-10/experiment_spec.md
- stage-10/validation_report.md
id: code_generation-rc-20260319-193805-15d264
run_id: rc-20260319-193805-15d264
stage: 10-code_generation
tags:
- code_generation
- stage-10
- run-rc-20260
title: 'Stage 10: Code Generation'
---

# Stage 10: Code Generation

Directory with 1 files: main.py

# Experiment Specification

## Topic
Parameter Golf val_bpb minimization — lane_3: QAT + roundtrip-aware compression alignment. Hypothesis: If we enable staged QAT aligned to int8+zlib roundtrip, post-roundtrip val_bpb will improve versus float-only training under the same gate budget. Artifact limit 16MB. Quick-gate: runtime <= 1.10x baseline.

## Project Structure
Multi-file experiment project with 1 file(s): `main.py`

## Entry Point
`main.py` — executed directly via sandbox

## Outputs
- `main.py` emits metric lines in `name: value` format
- Primary metric key: `val_bpb`

## Topic-Experiment Alignment
MISALIGNED: The visible portion of the code sets up an experiment that *intends* to evaluate QAT + roundtrip-aware compression for an int8+zlib export path in lane_3, but as given it does not yet constitute a complete or clearly comparative test of the stated hypothesis.

Topic/hypothesis recap:
- Topic: "Parameter Golf val_bpb minimization — lane_3: QAT + roundtrip-aware compression alignment."
- Hypothesis: Enabling staged QAT aligned to int8+zlib roundtrip should improve *post-roundtrip* val_bpb compared to float-only training, under the same gate budget.

What the code actually does:
1. It defines HYPERPARAMETERS that match a small transformer-like model (d_model=256, n_layer=8, n_head=8), which is consistent with a transformer/LLM-style lane rather than a toy MLP.
2. It enables a QAT-style condition via environment variables:
   - QAT_ENABLED=1, QAT_BITS=8, QAT_GROUP_SIZE=32, QAT_PER_CHANNEL=1, QAT_START_FRAC=0.5, QAT_SYMMETRIC=1.
   - COMPRESSION_REG_ENABLED=1, COMPRESSION_ROUNDTRIP_ENABLED=1, EXPORT_INT8=1, EXPORT_INT8_ZLIB=1.
   This is reasonably aligned with the idea of staged QAT plus some compression regularization and int8+zlib export, assuming train_gpt.py respects these env vars.
3. It explicitly ensures that a post-roundtrip evaluation is run by setting SKIP_FINAL_INT8_ROUNDTRIP_EVAL=0 and then parsing a dedicated val_bpb_roundtrip metric from stdout using a strict regex. That is directly relevant to "post-roundtrip val_bpb" in the topic.
4. It is wired to run via a safe distributed harness (_run_torch_distributed using harness.run_distributed), consistent with the broader infrastructure, and to use the lane_3 data pipeline via DATA_ROOT/DATASET_NAME/DATASET_CONFIG.
5. It defines multi-seed behavior (seeds = [42, 123, 456]) but the top-level driver that actually iterates over seeds and aggregates metrics is truncated. We only see run_single_seed.

Where alignment is missing or unclear:
1. No explicit float-only baseline condition in this code.
   - The hypothesis is a *comparison* of "staged QAT aligned to int8+zlib roundtrip" vs "float-only training" under the same gate/runtime budget.
   - The provided code only sets up a single configuration: QAT + compression_reg + roundtrip enabled. There is no alternate configuration in this file that disables QAT (QAT_ENABLED=0, EXPORT_INT8_ZLIB=0, etc.) while keeping the model and training budget identical.
   - The presence of baseline_val_bpb and baseline_train_time_ms in HYPERPARAMETERS indicates that the float-only baseline may come from a separate run, not from this script. If that baseline is produced elsewhere, this script alone does not actually *test* the hypothesis; it only runs one side of the comparison.

2. No internal gate/runtime check is enforced here.
   - quick_gate_runtime_factor=1.10 is logged but never used to compare this run’s actual runtime against the baseline. There is timing in run_single_seed (elapsed_ms is returned), but we do not see any logic that compares it to baseline_train_time_ms or enforces the <=1.10x constraint.
   - That means the code, as shown, does not implement the "Quick-gate: runtime <= 1.10x baseline" part of the research spec.

3. Incomplete/hidden core method implementation.
   - The actual implementation of "staged QAT", "compression_reg", and "roundtrip-aware" behavior is pushed into train_gpt.py and whatever quantization/compression code it uses. In this file we only set flags.
   - From a topic-experiment alignment standpoint, that is acceptable *if* train_gpt.py actually:
     * switches from float to QAT at QAT_START_FRAC of training,
     * applies a regularizer tied to compressibility (compression_reg_lambda, compression_reg_period),
     * performs a true export to int8 and then zlib-compresses, and
     * evaluates val_bpb_roundtrip on that compressed-decompressed model.
   - But this is not verifiable from the provided snippet. All we can say is that the code is *intended* to align with those methods via environment variables.

4. No explicit multi-condition structure for the experiment.
   - The research topic implies at least two conditions: (A) float-only training (baseline) and (B) QAT+roundtrip-aware training, with all other factors matched.
   - This script defines only the QAT+roundtrip condition. There is no conditional branch or configuration toggle to run both conditions within th

... (truncated, see full artifact)


# Code Validation Report

**Status**: PASSED after 1 total repair(s)

- File main.py attempt 1: Code validation: 2 error(s), 1 warning(s)