---
created: '2026-03-19T21:57:33+00:00'
evidence:
- stage-15/decision.md
- stage-15/decision_structured.json
id: research_decision-rc-20260319-212944-15d264
run_id: rc-20260319-212944-15d264
stage: 15-research_decision
tags:
- research_decision
- stage-15
- run-rc-20260
title: 'Stage 15: Research Decision'
---

# Stage 15: Research Decision

## Decision

REFINE

## Justification

The experiment clearly fails the minimum bar to PROCEED:

1. Only 1 run per condition (n=1), so criterion 3 (≥3 seeds) is not met.
2. Ablation integrity is explicitly broken and the tooling is failing, so we cannot trust that conditions differ as intended.
3. The analysis quality rating is 3/10, below the required ≥4/10.

By the decision rule you specified, any failure of these criteria forces REFINE, not PROCEED.

At the same time, the core hypotheses and metric design remain conceptually sound. The issues are with experimental wiring, ablation integrity, and benchmark saturation/degeneracy—these are fixable without discarding the idea. That makes PIVOT unwarranted: the hypothesis is not fundamentally invalidated, we just lack valid evidence.

The “DEGENERATE REFINE CYCLE” warning is important, but it is advisory. Your hard rule is that failing any minimum criterion blocks PROCEED. Given that, and that the underlying research question is still meaningful, the correct program-level call is to REFINE, but to treat this as a *non-local* refinement: fix the benchmark and infrastructure, not just hyperparameters.

## Evidence

- **Criterion violations for PROCEED:**
  - n=1 for all conditions (`baseline_float_lane3`, `QAT_only`, `H1_minimal_self_organizing_QAT`), so no multi-seed replication.
  - Explicit ablation failure warnings: “differentiating parameter is likely not used in the code.”
  - Analysis quality rating = **3/10**.
- **Tooling failures:**
  - Main run crashes: `ModuleNotFoundError: No module named 'harness'`.
- **Degeneracy signal:**
  - Metrics across two refine iterations: [3.35944261, 3.35844261] — effectively identical; indicates benchmark saturation or wiring error, not a stable, validated effect.

These show we lack valid evidence, but they do not logically refute the core idea (roundtrip-aware QAT for int8+zlib under constraints).

## Next Actions

Treat “REFINE” here as “fix the experimental platform and benchmark, not just rerun with tiny tweaks.”

### 1. Repair Infrastructure and Wiring

1.1. **Fix the harness import and run a full E2E sanity pass**

- Resolve `ModuleNotFoundError: No module named 'harness'`.
- Add a minimal integration test that:
  - Trains for a few hundred steps.
  - Logs `val_bpb`, `val_bpb_roundtrip`, `artifact_bytes`, `runtime_ratio`.
  - Confirms end-to-end success for each condition flag.

1.2. **Make condition toggles auditable**

- At run start, log:
  - Active mode: `baseline`, `QAT_only`, `H1_minimal_self_organizing_QAT`.
  - Key hyperparameters that *must* differ between modes (e.g., QAT loss terms, quantization schedule).
- Add assertions in code paths:
  - E.g., `assert use_qat_loss == (mode in {"QAT_only", "H1"})`.

1.3. **Harden ablation checks**

- Implement a simple automated check:
  - After a short training run (e.g., 1k steps), compare:
    - `val_bpb` and `val_bpb_roundtrip` across conditions.
  - If all are identical to machine precision or differ by <1e-6, abort with a “wiring failure” error.
- Also compare configuration diffs to ensure intended parameters are actually set differently per condition.

### 2. Address Degenerate / Saturated Behavior

2.1. **Confirm whether the benchmark is too easy or mis-logged**

- Inspect training curves from the two “identical” iterations:
  - Are loss curves genuinely identical, or is the logging truncated / cached?
- Check:
  - Dataset shuffling and seed handling.
  - Whether evaluation is accidentally reading a cached checkpoint or metrics file.

2.2. **Stress-test with perturbed configs**

- Run a small grid where you deliberately vary:
  - Learning rate by ±20%.
  - QAT loss weight by ±50%.
  - Quantization schedule (earlier vs later).
- If metrics remain numerically identical, it’s a strong sign:
  - Either the benchmark is trivial or
  - The evaluation / logging is not hooked into the actual model state.
- Fix accordingly (very likely the latter).

### 3. Normalize Baselines and Gates

3.1. **Define a single canonical float baseline**

- Freeze one baseline configuration per architecture:
  - Fixed seeds (e.g., 3 canonical seeds).
  - Fixed training schedule and data splits.
- Log one set of baseline metrics:
  - `baseline_val_bpb`, `baseline_runtime_ms`, `baseline_artifact_bytes`.

3.2. **Recompute all ratios against this baseline**

- For each condition:
  - `runtime_ratio = runtime_ms / baseline_runtime_ms`.
  - `artifact_ratio = artifact_bytes / baseline_artifact_bytes`.
- Ensure `quick_gate_passed` is purely a deterministic function of:
  - `runtime_ratio <= 1.10` and `artifact_bytes <= size_cap`.

3.3. **Add a unit test for gate logic**

- Synthetic metrics input → expected gate result, so we never again see:
  - H1 with `runtime_ratio` 1.1713 marked as gate-passing.

### 4. Run a Proper, Minimal Study

Once wiring and degeneracy issues are fixed:

4.1. **Multi-seed, minimal trio**

- Conditions:
  - `baseline_float_lane3`
  - `QAT_only`
  - `H1_minimal_self_o

... (truncated, see full artifact)


{
  "decision": "refine",
  "raw_text_excerpt": "## Decision\n\nREFINE\n\n## Justification\n\nThe experiment clearly fails the minimum bar to PROCEED:\n\n1. Only 1 run per condition (n=1), so criterion 3 (\u22653 seeds) is not met.\n2. Ablation integrity is explicitly broken and the tooling is failing, so we cannot trust that conditions differ as intended.\n3. The analysis quality rating is 3/10, below the required \u22654/10.\n\nBy the decision rule you specified, any failure of these criteria forces REFINE, not PROCEED.\n\nAt the same time, the core hypotheses and ",
  "quality_warnings": [],
  "generated": "2026-03-19T21:57:33+00:00"
}