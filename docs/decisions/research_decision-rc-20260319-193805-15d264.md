---
created: '2026-03-19T20:16:58+00:00'
evidence:
- stage-15/decision.md
- stage-15/decision_structured.json
id: research_decision-rc-20260319-193805-15d264
run_id: rc-20260319-193805-15d264
stage: 15-research_decision
tags:
- research_decision
- stage-15
- run-rc-20260
title: 'Stage 15: Research Decision'
---

# Stage 15: Research Decision

## Decision

REFINE

## Justification

- The current run produced **no experimental metrics** (no `val_bpb`, no artifact sizes, no runtimes).  
- Multiple **minimum quality criteria for PROCEED are violated**:
  - No baselines or proposed method have results (fails #1).
  - The primary metric is only conceptually defined, not actually logged (fails #2 in practice).
  - Zero seeds completed (fails #3).
  - No per-condition values exist, so ablation integrity can’t be checked (fails #4).
  - The analysis quality rating is **2/10**, below the ≥4 threshold (fails #5).
- The **research direction and hypotheses remain sound**: codec-aligned QAT under deployment-like constraints is coherent and promising; there is no evidence they are fundamentally flawed.  
- The issues are **implementation robustness and experimental design**, not conceptual dead-ends. This calls for refining the pipeline, baselines, and methodology rather than abandoning or rethinking the core ideas.

Given the explicit rule that if any PROCEED criterion is unmet we must choose REFINE (not PROCEED), and given there is no evidence that warrants discarding the hypotheses, REFINE is the warranted choice.

## Evidence

- **No results from any condition or seed**:
  - Run crashed inside project code before logging any metrics.
  - Planned 3 seeds; 0 completed.
- **Infrastructure behavior but no science yet**:
  - Time/seed gating triggered correctly (`SEED_WARNING`) and honored time budgets.
  - End-to-end codec path (float → INT8 → zlib → de-zlib → INT8 inference) is conceptually wired but not robust.
- **Methodology only partially instantiated**:
  - No concrete baselines with logged outcomes (float+PTQ, format-first QAT, etc.).
  - No ablations implemented and run.
- **Result quality rating: 2/10**:
  - Explicitly below the ≥4/10 bar required for PROCEED.
  - Rated as pipeline shakeout, not experimental evidence.

## Next Actions

### 1. Make the pipeline robust and observable

1.1 **Fix the crash in the project code**  
- Reproduce locally with a single seed and smallest possible model/dataset.  
- Add assertions and clear error logging around:
  - Quantization: scale/zero-point computation, type casts.
  - Serialization: weight layout, headers, and zlib call.
  - Deserialization: buffer lengths, dtype/layout matches, reshaping.

1.2 **Add a minimal codec sanity test (must-pass integration test)**  
- Configuration:  
  - Tiny model, tiny dataset.  
  - Float training → PTQ → zlib → de-zlib → INT8 inference.  
- Required logs (per seed):
  - Post-roundtrip `val_bpb`.
  - Compressed artifact size (bytes).
  - Total runtime.  
- Run across at least 3 seeds to confirm:
  - No crashes.
  - Stable metric logging.
  - Reasonable variability.

### 2. Lock in baselines and metrics

2.1 **Implement and wire the baseline grid**

Define these as first-class configurations:

- **B0 — Float + naive PTQ + zlib**
  - Standard float training.
  - Simple PTQ after training (per-tensor or per-channel, fixed scheme).
  - zlib compression; post-roundtrip evaluation.

- **B1 — Float + entropy-shaped INT8 layout + PTQ/light QAT + zlib**
  - Same training budget as B0.
  - Structured/entropy-aware INT8 layout, but still primarily PTQ or very light QAT.
  - Same codec settings.

- **B2 — Staged QAT without codec-in-loop**
  - Same format as B1.
  - QAT schedule applied, but training sees quantization only (no codec calls in the loss).

- **E0 — Target method: staged QAT with codec-in-loop**
  - Same as B2 but with codec-in-loop objective active during training.

All must share:
- Same datasets and splits.
- Same total training budget (steps/epochs).
- Identical quantization schemes where applicable.

2.2 **Standardize metrics and logging**

For each run/seed/config, log at minimum:

- **Primary**:
  - Post-roundtrip `val_bpb` (direction: lower is better; units: bits per byte or bits per symbol, clearly stated).

- **Secondary**:
  - Model artifact size (compressed bytes).
  - Weight compression ratio.
  - Total training runtime and inference runtime.
  - Number and aggregate time of codec-in-loop calls.

Ensure:
- Metrics are written at end-of-run even on partial failures when possible.
- Seeds, config IDs, and code commit hashes are logged.

### 3. Strengthen experimental design

3.1 **Data splits and evaluation protocol**

- Fix a **train / val / test** split:
  - Train: optimization only.
  - Val: early stopping / gross hyperparameter screening.
  - Test: used once per config for final reporting.
- Ensure the **roundtrip path matches deployment**:
  - Same serialization format and zlib parameters.
  - Same quantization scheme as expected at deployment.

3.2 **Seed strategy and variance estimation**

- After pipeline is stable:
  - Run **3–5 seeds per condition** (B0, B1, B2, E0) as a **pilot**.
  - Use pilot to:
    - Estimate variance of post-roundtrip `val_bpb` and runtimes.
    - Inform required n for a practically meaningful Δ`val_bpb` (e.g., 0.01 bpb).

- Use *

... (truncated, see full artifact)


{
  "decision": "refine",
  "raw_text_excerpt": "## Decision\n\nREFINE\n\n## Justification\n\n- The current run produced **no experimental metrics** (no `val_bpb`, no artifact sizes, no runtimes).  \n- Multiple **minimum quality criteria for PROCEED are violated**:\n  - No baselines or proposed method have results (fails #1).\n  - The primary metric is only conceptually defined, not actually logged (fails #2 in practice).\n  - Zero seeds completed (fails #3).\n  - No per-condition values exist, so ablation integrity can\u2019t be checked (fails #4).\n  - The a",
  "quality_warnings": [],
  "generated": "2026-03-19T20:16:58+00:00"
}