---
created: '2026-03-19T19:50:33+00:00'
evidence:
- stage-09/exp_plan.yaml
id: experiment_design-rc-20260319-193805-15d264
run_id: rc-20260319-193805-15d264
stage: 09-experiment_design
tags:
- experiment_design
- stage-09
- run-rc-20260
title: 'Stage 09: Experiment Design'
---

# Stage 09: Experiment Design

constraints:
  artifact_max_bytes: 16000000
  notes: 'Single quick-gate candidate for lane_3. Uses staged QAT aligned to int8+zlib
    deployment with light compression regularization. Must be directly comparable
    to the lane_3 float-only baseline under identical data/model/optimizer.

    '
  quick_gate_runtime_factor: 1.1
  time_budget_seconds: 1200
environment:
  ARTIFACT_MAX_BYTES: '16000000'
  COMPRESSION_REG_ENABLED: '1'
  COMPRESSION_REG_LAMBDA: 1e-4
  COMPRESSION_REG_PERIOD: '200'
  COMPRESSION_ROUNDTRIP_ENABLED: '1'
  DATASET_CONFIG: wikitext-2-raw-v1
  DATASET_NAME: wikitext
  DATA_ROOT: /workspace/data
  D_MODEL: '256'
  EVAL_INTERVAL: '100'
  EXPERIMENT_ID: h1_entropy_shaped_qat_vs_codec_in_loop_baseline_alignment_run1
  EXPORT_INT8: '1'
  EXPORT_INT8_ZLIB: '1'
  LANE_ID: lane_3
  LR: 3e-4
  LR_SCHEDULE: cosine
  MAX_STEPS: '500'
  MAX_TOKENS: '65536'
  MODEL_SIZE: small
  NUM_WORKERS: '4'
  N_HEAD: '8'
  N_LAYER: '8'
  OPTIMIZER: adamw
  QAT_BITS: '8'
  QAT_ENABLED: '1'
  QAT_GROUP_SIZE: '32'
  QAT_PER_CHANNEL: '1'
  QAT_START_FRAC: '0.5'
  QAT_SYMMETRIC: '1'
  SETUP_SCRIPT: /workspace/setup.py
  WARMUP_STEPS: '50'
  WEIGHT_DECAY: '0.01'
hypothesis_id: H1_entropy_shaped_vs_codec_in_loop_QAT_baseline_alignment
knobs:
  compression_roundtrip:
    enabled: true
    export_int8: true
    export_int8_zlib: true
    period_steps: 200
    reg_enabled: true
    reg_lambda: 0.0001
  data:
    dataset: wikitext-2-raw-v1
    root: /workspace/data
    setup_script: /workspace/setup.py
  description: 'This run instantiates the "QAT_only + light compression_reg" corner
    for Hypothesis 1, serving as the codec-aligned staged-QAT baseline against which
    entropy-shaped INT8 formats will later be compared. All knobs are chosen to (1)
    keep runtime within 1.10x baseline, (2) ensure the export path is int8+zlib, and
    (3) minimally perturb the existing training recipe beyond enabling staged QAT.

    '
  model:
    d_model: 256
    n_head: 8
    n_layer: 8
  qat:
    bits: 8
    enabled: true
    group_size: 32
    per_channel: true
    start_frac: 0.5
    symmetric: true
  training_schedule:
    eval_interval: 100
    max_steps: 500
    max_tokens: 65536
lane_id: lane_3
run_command: 'torchrun --standalone --nproc_per_node=1 /parameter-golf/train_gpt.py

  '
success_criteria:
  details: "The run is a quick-gate candidate. It succeeds if: (a) final reported\
    \ val_bpb after exporting and reloading the int8+zlib artifact\n    is strictly\
    \ lower than the lane_3 float-only baseline val_bpb,\n(b) measured training runtime\
    \ (runtime_ms) is ≤ 1.10× the float-only baseline runtime, and (c) the serialized\
    \ int8+zlib artifact size is < 16,000,000 bytes. Subsequent promotion requires\
    \ confirming val_bpb on the actual .int8.ptz roundtrip."
  objective: minimize
  primary_metric: val_bpb
  quick_gate:
    artifact_bytes_max: 16000000
    must_improve_bpb: true
    runtime_factor: 1.1
topic: 'Parameter Golf val_bpb minimization — lane_3: QAT + roundtrip-aware compression
  alignment. Hypothesis: If we enable staged QAT aligned to int8+zlib roundtrip, post-roundtrip
  val_bpb will improve versus float-only training under the same gate budget. Artifact
  limit 16MB. Quick-gate: runtime <= 1.10x baseline.'
