---
created: '2026-03-19T18:54:05+00:00'
evidence:
- stage-03/search_plan.yaml
- stage-03/sources.json
- stage-03/queries.json
id: search_strategy-rc-20260319-185145-15d264
run_id: rc-20260319-185145-15d264
stage: 03-search_strategy
tags:
- search_strategy
- stage-03
- run-rc-20260
title: 'Stage 03: Search Strategy'
---

# Stage 03: Search Strategy

constraints:
  artifact_size_cap_mb: 16
  compression:
    alignment: train-time proxies or layout constraints that correlate with zlib-compressed
      size
    codec:
    - zlib
    - DEFLATE
  dataset_focus:
    primary:
    - enwik8
    - Hutter Prize
    - byte-level language modeling
    secondary:
    - text8
    - character-level LM
    - byte-level Transformer
  max_train_time_ratio: 1.1
  quantization:
    quantization_style:
    - QAT
    - post-training quantization
    - fake quantization
    target_precision: int8
extraction_plan:
  common_fields:
  - model_architecture
  - parameter_count
  - dataset
  - metric_type
  - metric_value
  - quantization_type
  - quant_scheme
  - checkpoint_size_bytes
  - compression_codec
  - compressed_size_bytes
  - training_overhead_factor
  - evaluation_overhead_notes
  - regularization_or_proxy
  - code_or_repo_url
  synthesis:
  - compare_enwik8_frontiers: Synthesize typical enwik8 bpb/bpc results for models
      that could plausibly be shrunk to ≤16MB with int8+zlib, and identify reasonable
      baseline targets.
  - quantify_qat_overheads: Summarize reported QAT slowdowns and choose candidate
      staged-QAT schedules that can likely meet ≤1.10× training time.
  - identify_promising_proxies: List differentiable statistics that correlate with
      entropy/compressibility and seem cheap enough to add during QAT.
  - design_experimental_grid: Based on the literature, propose a small grid of architectures,
      QAT schedules, and proxy strengths to test the central hypothesis under the
      constraints.
objective: Determine whether staged, roundtrip-aware QAT for small Transformer LMs
  (e.g., on enwik8) can improve post-roundtrip validation bits-per-byte (val_bpb)
  compared to float-only training under a fixed gate budget (artifact ≤16MB, training
  runtime ≤1.10× baseline), with explicit alignment to an int8 + zlib deployment pipeline.
phases:
- goal: Establish float and naive-int8+zlib baselines under the 16MB artifact and
    1.10× runtime constraints; understand typical enwik8 val_bpb and model-size frontiers.
  id: phase_q1_baseline_feasibility
  priority: 1
  queries:
  - id: q1_enwik8_transformer_baselines
    intent: Find state-of-the-art and small-model Transformer / attention-based baselines
      on enwik8 (or closely related byte-level LM tasks), including parameter counts,
      model sizes, and val_bpb or bpc.
    notes: 'Extract: dataset variant, model architecture, parameter count, reported
      bpb/bpc, checkpoint size if given, and any mention of quantization or compression.'
    query: ("enwik8" OR "Hutter Prize") AND ("Transformer" OR "attention" OR "GPT"
      OR "autoregressive model") AND ("bits-per-character" OR "bits per byte" OR bpb
      OR bpc OR "validation bpb") AND ("small" OR "compact" OR "parameter-efficient"
      OR "low-parameter")
    sources:
    - scholar
    - arxiv
    - openreview
  - id: q1_int8_post_training_quantization
    intent: Understand typical degradation from float32 to naive int8 post-training
      quantization for language models, especially under per-tensor or per-channel
      quantization without QAT.
    notes: Look for results where authors report accuracy/perplexity drops and model
      sizes for int8 PTQ vs float. If possible, prioritize works that mention deployment
      artifacts or serialized model sizes.
    query: ("post-training quantization" OR "PTQ") AND ("Transformer" OR "language
      model" OR "BERT" OR "GPT") AND (int8 OR "8-bit") AND ("per-channel" OR "per-tensor")
      AND ("perplexity" OR "bits-per-character" OR "bits per byte" OR bpb)
    sources:
    - scholar
    - arxiv
  - id: q1_model_size_vs_params
    intent: Obtain rules of thumb for mapping parameter counts to artifact size in
      float32, float16, and int8, including any overhead from metadata and tensor
      layout.
    notes: Use to back-of-the-envelope what architectures can fit under the 16MB cap
      for float vs quantized deployments.
    query: ("model size" OR "checkpoint size" OR "artifact size") AND ("int8" OR "8-bit"
      OR "float16" OR "FP16") AND ("Transformer" OR "language model") AND ("parameters"
      OR "parameter count")
    sources:
    - scholar
    - web
  - id: q1_zlib_on_model_weights
    intent: Identify any empirical studies or engineering notes about using zlib/DEFLATE
      to compress serialized neural network weights, including how layout, ordering,
      and padding affect compressed size.
    notes: Even blog posts or framework docs (PyTorch, TensorFlow, ONNX) that discuss
      checkpoint compression with zlib are acceptable; extract any quantitative results
      and layout recommendations.
    query: ("zlib" OR "DEFLATE") AND ("neural network weights" OR "model checkpoint"
      OR "Tensor" OR "state_dict") AND ("compression" OR "compressed size")
    sources:
    - web
    - scholar
  - id: q1_runtime_overhead_roundtrip
    intent: Estimate training-time cost of perio

... (truncated, see full artifact)


{
  "sources": [
    {
      "id": "scholar",
      "name": "Google Scholar",
      "type": "academic_search",
      "url": "https://scholar.google.com",
      "status": "planned",
      "query": "see search_plan_yaml: use per-query boolean strings for scholarly literature on enwik8 baselines, QAT, int8 quantization, and compression-aware training",
      "verified_at": null
    },
    {
      "id": "arxiv",
      "name": "arXiv",
      "type": "preprint_server",
      "url": "https://arxiv.org",
      "status": "planned",
      "query": "see search_plan_yaml: replicate scholar queries with arXiv's advanced search, filter by cs.LG, cs.CL, cs.CV, stat.ML",
      "verified_at": null
    },
    {
      "id": "openreview",
      "name": "OpenReview",
      "type": "conference_repository",
      "url": "https://openreview.net",
      "status": "planned",
      "query": "see search_plan_yaml: target ICLR/NeurIPS submissions on QAT, int8 Transformers, and model compression",
      "verified_at": null
    },
    {
      "id": "web",
      "name": "General Web Search",
      "type": "web_search",
      "url": "https://www.google.com",
      "status": "planned",
      "query": "see search_plan_yaml: use for engineering notes on zlib-compressed checkpoints, weight layout, and runtime overhead anecdotes",
      "verified_at": null
    },
    {
      "id": "pytorch_docs",
      "name": "PyTorch Quantization Docs",
      "type": "framework_docs",
      "url": "https://pytorch.org/docs/stable/quantization.html",
      "status": "planned",
      "query": "int8 quantization-aware training Transformer runtime overhead fake quant observers",
      "verified_at": null
    },
    {
      "id": "tf_model_optimization",
      "name": "TensorFlow Model Optimization Toolkit",
      "type": "framework_docs",
      "url": "https://www.tensorflow.org/model_optimization",
      "status": "planned",
      "query": "quantization aware training int8 runtime overhead staged qat",
      "verified_at": null
    },
    {
      "id": "onnx_runtime",
      "name": "ONNX Runtime Quantization",
      "type": "framework_docs",
      "url": "https://onnxruntime.ai/docs/performance/quantization.html",
      "status": "planned",
      "query": "int8 quantization transformer per-channel per-tensor",
      "verified_at": null
    }
  ],
  "count": 7,
  "generated": "2026-03-19T18:54:05+00:00"
}

{
  "queries": [
    "Parameter Golf val bpb minimization lane",
    "Parameter Golf val bpb benchmark",
    "Parameter Golf val bpb survey",
    "Golf val bpb minimization",
    "Parameter Golf val comparison",
    "Parameter Golf val deep learning",
    "val bpb minimization lane"
  ],
  "year_min": 2020
}