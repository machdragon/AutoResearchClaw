---
created: '2026-03-19T18:59:08+00:00'
evidence:
- stage-09/exp_plan.yaml
id: experiment_design-rc-20260319-185145-15d264
run_id: rc-20260319-185145-15d264
stage: 09-experiment_design
tags:
- experiment_design
- stage-09
- run-rc-20260
title: 'Stage 09: Experiment Design'
---

# Stage 09: Experiment Design

constraints:
  quick_gate:
    artifact_bytes_max: 16000000
    runtime_factor: 1.1
  time_budget_seconds: 120
environment:
  ARTIFACT_PATH: /workspace/artifacts/model.int8.ptz
  ARTIFACT_SIZE_LIMIT: '16000000'
  BATCH_SIZE: '32'
  CC_ENABLE_FRAC: '0.7'
  CC_FEAT_LOSS_WEIGHT: '0.3'
  CC_KD_LOSS_WEIGHT: '0.3'
  CC_KD_TEMPERATURE: '2.0'
  CC_VIEW_ALIGN_LAYERS: last_block
  COMPRESSION_CONTRASTIVE_ENABLED: '1'
  COMPRESSION_PROXY_MODE: hist_entropy
  COMPRESSION_REG_ENABLED: '1'
  COMPRESSION_REG_LAMBDA: 1e-4
  COMPRESSION_REG_SCOPE: weights
  DATASET_NAME: wikitext-2
  DATASET_ROOT: /workspace/data
  DATASET_SPLIT_TRAIN: train
  DATASET_SPLIT_VALID: validation
  EXPORT_INT8: '1'
  EXPORT_ZLIB: '1'
  LOG_PRIMARY_METRIC: val_bpb
  LOG_RUNTIME_MS: '1'
  LR: 3e-4
  MAX_STEPS: auto
  MIXED_PRECISION: fp16
  MODEL_SIZE: small
  OPTIMIZER: adamw
  QAT_ACTIVATIONS: '1'
  QAT_ENABLED: '1'
  QAT_GROUP_SIZE: '32'
  QAT_START_FRAC: '0.5'
  SAVE_BEST_ONLY: '1'
  SEQ_LEN: '256'
  TOKENIZER_NAME: gpt2
  ZLIB_LEVEL: '6'
hypothesis_id: lane3_h1_compression_contrastive_qat_single_run
knobs:
  compression_contrastive:
    align_layers:
    - last_block
    enable_frac: 0.7
    enabled: true
    feat_loss_weight: 0.3
    kd_loss_weight: 0.3
    kd_temperature: 2.0
  compression_regularization:
    enabled: true
    lambda: 0.0001
    proxy_mode: hist_entropy
    scope: weights
  dataset:
    name: wikitext-2
    root: /workspace/data
    seq_len: 256
    splits:
      train: train
      valid: validation
  description: 'Single quick-gate candidate instantiating compression-contrastive
    staged QAT: - float-only warmup, then enabling weight+limited activation fake-quant
    at 50% of training - turning on a second "compressed view" forward and alignment
    losses only in the last 30% - light histogram-entropy regularization as a cheap
    zlib proxy All other knobs (model, optimizer, data pipeline) are inherited from
    the baseline config.

    '
  export:
    artifact_path: /workspace/artifacts/model.int8.ptz
    artifact_size_limit: 16000000
    int8: true
    zlib: true
    zlib_level: 6
  qat:
    activations: true
    enabled: true
    group_size: 32
    start_frac: 0.5
lane_id: lane_3
run_command: 'torchrun --standalone --nproc_per_node=1 /parameter-golf/train_gpt.py

  '
success_criteria:
  goal: minimize
  primary_metric: val_bpb
  quick_gate:
    artifact_bytes_max: 16000000
    must_improve_bpb: true
    runtime_factor: 1.1
topic: 'Parameter Golf val_bpb minimization — lane_3: QAT + roundtrip-aware compression
  alignment. Hypothesis: If we enable staged QAT aligned to int8+zlib roundtrip, post-roundtrip
  val_bpb will improve versus float-only training under the same gate budget. Artifact
  limit 16MB. Quick-gate: runtime <= 1.10x baseline.'
